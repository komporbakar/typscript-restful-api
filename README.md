# Setup Project

Create .env file

```json
DATABASE_URL="mysql://root:@localhost:3306/typescript-restful-api"
```

```shell

npm install

npx prisma migrate dev

npx prisma generate

npm run build

npm run start

```
